/*
 * Host-test stub for <arch/zxn.h>.
 *
 * The commands include <arch/zxn.h> for the z88dk/ZX Next target. When the
 * portable C logic is compiled on the host (see test/run.sh) this stub stands
 * in for it. The only thing the pure transformers (number, morse) need from it
 * is the fixed-width integer types, so we just pull in <stdint.h>.
 */
#ifndef DOTS_TEST_STUB_ARCH_ZXN_H
#define DOTS_TEST_STUB_ARCH_ZXN_H

#include <stdint.h>

#endif
